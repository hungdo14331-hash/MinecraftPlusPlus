{
  "name": "minecraft-plus-plus",
  "version": "0.3.3",
  "private": true,
  "description": "Minecraft++ — Bedrock addon combat overhaul (HP x5, Combat Core, Target HUD)",
  "type": "module",
  "scripts": {
    "bump": "node tools/bump-version.mjs",
    "manifests": "node tools/generate-manifests.mjs",
    "build": "npm run manifests && node build.mjs",
    "package": "npm run bump && npm run build && npm run audit && node tools/package-addon.mjs",
    "audit": "node tools/audit-content.mjs",
    "test": "npm run audit"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "esbuild": "^0.28.1",
    "typescript": "^5.9.3"
  }
}
