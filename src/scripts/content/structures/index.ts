import { StructureRegistry } from "../../core/registry/registries";

export function register(): void {
  // Chua co structure content nao duoc chot — de trong nhu ban goc.
  void StructureRegistry;
}
